{
  "name": "serie-a-tv-audience",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "preview:full": "npm run preview:full --prefix client",
    "generate:crests": "node scripts/generate-crests.mjs"
  }
}
